import React from 'react';import ReactDOM from 'react-dom/client';
function App(){return <div style={{fontFamily:'sans-serif',padding:20}}><h1>Employee Lifecycle Automation Governance Agent</h1><p>Backend health and login driven platform scaffold is running.</p></div>}
ReactDOM.createRoot(document.getElementById('root')!).render(<App/>);
