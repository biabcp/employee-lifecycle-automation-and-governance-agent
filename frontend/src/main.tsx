import React from 'react';import ReactDOM from 'react-dom/client';
function App(){return <div style={{fontFamily:'sans-serif',padding:20}}><h1>Employee Lifecycle Automation Governance Agent</h1><p>Backend health and login driven employee lifecycle governance platform is running in local demo mode.</p></div>}
ReactDOM.createRoot(document.getElementById('root')!).render(<App/>);
